/**
 * Marvel Multiverse Timeline - Legacy Entrypoint
 * Bridges js/graphics.js and js/data.js for backwards compatibility.
 */

// If running in browser where js/graphics.js or js/data.js may be included
if (typeof document !== 'undefined') {
  // Ensured compatibility layer
}